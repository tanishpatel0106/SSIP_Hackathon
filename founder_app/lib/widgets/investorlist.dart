import 'package:flutter/material.dart';

class InvestorList extends StatelessWidget {
  const InvestorList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Investor List'),
    );
  }
}
